#ifndef Theme_H
#define Theme_H

#include <string>

std::string currentTime();
std::string currentTimeMs();
int64_t timestamp();
int64_t timestamp_micro();
bool isQtCreatorParentProc();

#endif // Theme_H
